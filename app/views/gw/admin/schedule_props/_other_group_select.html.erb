<%
javascript_fname = 'search_group_search_submit();'
grouplist = (build_select_parent_groups).unshift(["すべて", 0])
%>
<%= form_tag '', :method => :get, :class => 'search', :name => 'search' do -%>
  <%= hidden_field_tag('s_genre', params[:s_genre]) unless params[:s_genre].blank? -%>
  <%= hidden_field_tag('s_date', params[:s_date]) unless params[:s_date].blank? -%>
  <%= hidden_field_tag('id', params[:id]) unless params[:id].blank? -%>
  <%= hidden_field_tag('cls', params[:cls]) unless params[:cls].blank? -%>
<%#種別 <%= select_tag('type_id' , options_from_collection_for_select(@prop_types, :id, :name, @type_id), :onchange=>javascript_fname ) -%>
  種別・グループ <%= select_tag('type_id' , options_for_select(@prop_types, @type_id), :onchange=>javascript_fname ) -%>
  施設管理所属 <%= select_tag('s_other_admin_gid' , options_for_select(grouplist, @s_other_admin_gid), :onchange=>javascript_fname ) -%>
<% end -%>
